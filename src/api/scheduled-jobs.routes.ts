/**
 * Scheduled Jobs API Routes
 *
 * Re-exports from the split module directory.
 * The actual handlers live in ./scheduled-jobs/*.ts
 *
 * @module api/scheduled-jobs.routes
 */

export { handleScheduledJobsRoutes } from './scheduled-jobs/index.js';
