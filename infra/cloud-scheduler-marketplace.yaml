# Cloud Scheduler Jobs for Marketplace Billing
#
# ALL jobs removed — no API handlers implemented (audit 2026-02-23):
# - marketplace-daily-aggregation (target: /api/jobs/marketplace-daily-aggregation — no handler)
# - marketplace-weekly-reports (target: /api/jobs/marketplace-weekly-reports — no handler)
# - marketplace-monthly-revenue (target: /api/jobs/marketplace-monthly-revenue — no handler)
# - marketplace-publisher-payouts (target: /api/jobs/marketplace-publisher-payouts — no handler)
# - marketplace-quarterly-cleanup (target: /api/jobs/marketplace-quarterly-cleanup — no handler)
# Re-add when handlers are implemented.
#
# Deploy all jobs with:
#   ./scripts/deploy-marketplace-scheduler.sh
#
# Or individually with gcloud commands below.

jobs: []
