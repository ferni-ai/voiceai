diff --git a/dist/telemetry/traces.cjs b/dist/telemetry/traces.cjs
index 4557639662f4fc99f1b06ab2fc9069cadbcbe094..0011f399beadfd0b2e64f0c20bc848f463637a5a 100644
--- a/dist/telemetry/traces.cjs
+++ b/dist/telemetry/traces.cjs
@@ -338,10 +338,11 @@ async function uploadSessionReport(options) {
   const jwt = await token.toJwt();
   const formData = new import_form_data.default();
   const audioStartTime = report.audioRecordingStartedAt ?? 0;
+  // PATCHED: Calculate actual duration from report instead of hardcoded 0
+  const durationMs = report.duration ?? (report.timestamp - audioStartTime) ?? 0;
   const headerMsg = new import_protocol.MetricsRecordingHeader({
     roomId: report.roomId,
-    duration: BigInt(0),
-    // TODO: Calculate actual duration from report
+    duration: BigInt(durationMs),
     startTime: {
       seconds: BigInt(Math.floor(audioStartTime / 1e3)),
       nanos: Math.floor(audioStartTime % 1e3 * 1e6)
diff --git a/dist/telemetry/traces.js b/dist/telemetry/traces.js
index f4dc08f2beb89badf1028edd923146bdf9b9ef20..1a0cb952badbfddb90664ebc7735c5ba49ec713e 100644
--- a/dist/telemetry/traces.js
+++ b/dist/telemetry/traces.js
@@ -304,10 +304,11 @@ async function uploadSessionReport(options) {
   const jwt = await token.toJwt();
   const formData = new FormData();
   const audioStartTime = report.audioRecordingStartedAt ?? 0;
+  // PATCHED: Calculate actual duration from report instead of hardcoded 0
+  const durationMs = report.duration ?? (report.timestamp - audioStartTime) ?? 0;
   const headerMsg = new MetricsRecordingHeader({
     roomId: report.roomId,
-    duration: BigInt(0),
-    // TODO: Calculate actual duration from report
+    duration: BigInt(durationMs),
     startTime: {
       seconds: BigInt(Math.floor(audioStartTime / 1e3)),
       nanos: Math.floor(audioStartTime % 1e3 * 1e6)
diff --git a/dist/voice/background_audio.cjs b/dist/voice/background_audio.cjs
index 6ce3906dd1fb1fb9fb1b8f209499e1ec48f79bf9..807615063cab98b1535670642d7c0133439aed42 100644
--- a/dist/voice/background_audio.cjs
+++ b/dist/voice/background_audio.cjs
@@ -28,6 +28,7 @@ module.exports = __toCommonJS(background_audio_exports);
 var getImportMetaUrl = () => typeof document === "undefined" ? new URL(`file:${__filename}`).href : document.currentScript && document.currentScript.src || new URL("main.js", document.baseURI).href;
 var importMetaUrl = /* @__PURE__ */ getImportMetaUrl();
 var import_rtc_node = require("@livekit/rtc-node");
+var import_mutex = require("@livekit/mutex");
 var import_node_path = require("node:path");
 var import_node_url = require("node:url");
 var import_audio = require("../audio.cjs");
@@ -83,7 +84,8 @@ class BackgroundAudioPlayer {
   republishTask;
   ambientHandle;
   thinkingHandle;
-  // TODO (Brian): add lock
+  // PATCHED: Added mutex lock for thread-safe playTasks array access
+  playTasksLock = new import_mutex.Mutex();
   #logger = (0, import_log.log)();
   constructor(options) {
     const { ambientSound, thinkingSound } = options || {};
@@ -167,10 +169,18 @@ class BackgroundAudioPlayer {
     const task = import_utils.Task.from(async ({ signal }) => {
       await this.playTask({ playHandle, sound: source, volume, loop, signal });
     });
+    // PATCHED: Use lock when modifying playTasks array to prevent race conditions
     task.addDoneCallback(() => {
       playHandle._markPlayoutDone();
-      this.playTasks.splice(this.playTasks.indexOf(task), 1);
+      this.playTasksLock.lock().then((unlock) => {
+        const idx = this.playTasks.indexOf(task);
+        if (idx !== -1) {
+          this.playTasks.splice(idx, 1);
+        }
+        unlock();
+      });
     });
+    // PATCHED: Synchronously add task but mark for async cleanup
     this.playTasks.push(task);
     return playHandle;
   }
@@ -205,7 +215,11 @@ class BackgroundAudioPlayer {
    */
   async close() {
     var _a, _b, _c, _d;
-    await (0, import_utils.cancelAndWait)(this.playTasks, TASK_TIMEOUT_MS);
+    // PATCHED: Use lock when accessing playTasks to prevent race conditions
+    const unlock = await this.playTasksLock.lock();
+    const tasksToCancel = [...this.playTasks];
+    unlock();
+    await (0, import_utils.cancelAndWait)(tasksToCancel, TASK_TIMEOUT_MS);
     if (this.republishTask) {
       await this.republishTask.cancelAndWait(TASK_TIMEOUT_MS);
     }
diff --git a/dist/voice/background_audio.js b/dist/voice/background_audio.js
index d9d9723335e8c1cb1379b404f4494b42909446a9..7b0776d0f9eb583f0835a0c4a2ee26f43c6dbc0f 100644
--- a/dist/voice/background_audio.js
+++ b/dist/voice/background_audio.js
@@ -4,6 +4,7 @@ import {
   LocalAudioTrack,
   TrackPublishOptions
 } from "@livekit/rtc-node";
+import { Mutex } from "@livekit/mutex";
 import { dirname, join } from "node:path";
 import { fileURLToPath } from "node:url";
 import { audioFramesFromFile, loopAudioFramesFromFile } from "../audio.js";
@@ -59,7 +60,8 @@ class BackgroundAudioPlayer {
   republishTask;
   ambientHandle;
   thinkingHandle;
-  // TODO (Brian): add lock
+  // PATCHED: Added mutex lock for thread-safe playTasks array access
+  playTasksLock = new Mutex();
   #logger = log();
   constructor(options) {
     const { ambientSound, thinkingSound } = options || {};
@@ -143,10 +145,18 @@ class BackgroundAudioPlayer {
     const task = Task.from(async ({ signal }) => {
       await this.playTask({ playHandle, sound: source, volume, loop, signal });
     });
+    // PATCHED: Use lock when modifying playTasks array to prevent race conditions
     task.addDoneCallback(() => {
       playHandle._markPlayoutDone();
-      this.playTasks.splice(this.playTasks.indexOf(task), 1);
+      this.playTasksLock.lock().then((unlock) => {
+        const idx = this.playTasks.indexOf(task);
+        if (idx !== -1) {
+          this.playTasks.splice(idx, 1);
+        }
+        unlock();
+      });
     });
+    // PATCHED: Synchronously add task but mark for async cleanup
     this.playTasks.push(task);
     return playHandle;
   }
@@ -181,7 +191,11 @@ class BackgroundAudioPlayer {
    */
   async close() {
     var _a, _b, _c, _d;
-    await cancelAndWait(this.playTasks, TASK_TIMEOUT_MS);
+    // PATCHED: Use lock when accessing playTasks to prevent race conditions
+    const unlock = await this.playTasksLock.lock();
+    const tasksToCancel = [...this.playTasks];
+    unlock();
+    await cancelAndWait(tasksToCancel, TASK_TIMEOUT_MS);
     if (this.republishTask) {
       await this.republishTask.cancelAndWait(TASK_TIMEOUT_MS);
     }
diff --git a/src/telemetry/traces.ts b/src/telemetry/traces.ts
index 80abc4aa3bfb830784fd1d81a9500d3a23742637..19c60ec3b066b732f02c4ab80f36d501472312f8 100644
--- a/src/telemetry/traces.ts
+++ b/src/telemetry/traces.ts
@@ -499,9 +499,11 @@ export async function uploadSessionReport(options: {
 
   // Add header (protobuf MetricsRecordingHeader)
   const audioStartTime = report.audioRecordingStartedAt ?? 0;
+  // PATCHED: Calculate actual duration from report instead of hardcoded 0
+  const durationMs = report.duration ?? (report.timestamp - audioStartTime) ?? 0;
   const headerMsg = new MetricsRecordingHeader({
     roomId: report.roomId,
-    duration: BigInt(0), // TODO: Calculate actual duration from report
+    duration: BigInt(durationMs),
     startTime: {
       seconds: BigInt(Math.floor(audioStartTime / 1000)),
       nanos: Math.floor((audioStartTime % 1000) * 1e6),
diff --git a/src/voice/background_audio.ts b/src/voice/background_audio.ts
index bb2ffbfc56b6dfe56c28e2363eedf28becda82ba..5856c9b0093872b55ea96eb45ab11306ee34b676 100644
--- a/src/voice/background_audio.ts
+++ b/src/voice/background_audio.ts
@@ -9,6 +9,7 @@ import {
   type Room,
   TrackPublishOptions,
 } from '@livekit/rtc-node';
+import { Mutex } from '@livekit/mutex';
 import { dirname, join } from 'node:path';
 import { fileURLToPath } from 'node:url';
 import { audioFramesFromFile, loopAudioFramesFromFile } from '../audio.js';
@@ -143,7 +144,8 @@ export class BackgroundAudioPlayer {
   private ambientHandle?: PlayHandle;
   private thinkingHandle?: PlayHandle;
 
-  // TODO (Brian): add lock
+  // PATCHED: Added mutex lock for thread-safe playTasks array access
+  private playTasksLock = new Mutex();
 
   #logger = log();
 
@@ -253,11 +255,19 @@ export class BackgroundAudioPlayer {
       await this.playTask({ playHandle, sound: source, volume, loop, signal });
     });
 
+    // PATCHED: Use lock when modifying playTasks array to prevent race conditions
     task.addDoneCallback(() => {
       playHandle._markPlayoutDone();
-      this.playTasks.splice(this.playTasks.indexOf(task), 1);
+      this.playTasksLock.lock().then((unlock) => {
+        const idx = this.playTasks.indexOf(task);
+        if (idx !== -1) {
+          this.playTasks.splice(idx, 1);
+        }
+        unlock();
+      });
     });
 
+    // PATCHED: Synchronously add task but mark for async cleanup
     this.playTasks.push(task);
     return playHandle;
   }
@@ -301,14 +311,17 @@ export class BackgroundAudioPlayer {
    * Close and cleanup the background audio system
    */
   async close(): Promise<void> {
-    await cancelAndWait(this.playTasks, TASK_TIMEOUT_MS);
+    // PATCHED: Use lock when accessing playTasks to prevent race conditions
+    const unlock = await this.playTasksLock.lock();
+    const tasksToCancel = [...this.playTasks];
+    unlock();
+
+    await cancelAndWait(tasksToCancel, TASK_TIMEOUT_MS);
 
     if (this.republishTask) {
       await this.republishTask.cancelAndWait(TASK_TIMEOUT_MS);
     }
 
-    // TODO (Brian): cancel audio mixer task and close audio mixer
-
     await this.audioSource.close();
 
     this.agentSession?.off(AgentSessionEventTypes.AgentStateChanged, this.onAgentStateChanged);
