# Skip CUDA binaries for onnxruntime-node (not needed for Cloud Run)
# This prevents build failures when GitHub releases are temporarily unavailable
# The env var format for postinstall scripts
onnxruntime_node_install_cuda=skip
