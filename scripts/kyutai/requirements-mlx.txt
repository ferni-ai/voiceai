# Kyutai MLX bridge server – run real Moshi models on Mac (Apple Silicon).
# Install: pip install -r scripts/kyutai/requirements-mlx.txt
websockets>=12.0
numpy>=1.24.0
# Real STT/TTS inference (MLX on Mac). Requires Python 3.12 (not 3.13+) and Apple Silicon.
# Create venv with: python3.12 -m venv scripts/kyutai/.venv
websockets>=12.0
numpy>=1.24.0
moshi-mlx>=0.2.12
sentencepiece
huggingface_hub
