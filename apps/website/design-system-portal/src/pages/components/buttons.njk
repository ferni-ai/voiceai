---
layout: layouts/base.njk
title: Buttons Component
description: Primary, secondary, and ghost button variants for Ferni interfaces
permalink: /components/buttons/
---

<article class="component-page">
  <header class="page-header">
    <nav class="breadcrumb">
      <a href="/components/">Components</a>
      <span>/</span>
      <span>Buttons</span>
    </nav>
    <h1>Buttons</h1>
    <p class="lead">Action-oriented buttons designed to feel approachable and responsive. Every button provides clear feedback and accessibility.</p>
  </header>

  <section class="section">
    <h2>Button Variants</h2>
    <p>Three variants for different levels of visual emphasis.</p>

    <div class="demo-container">
      <div class="button-showcase">
        <button class="btn btn-primary">Primary</button>
        <button class="btn btn-secondary">Secondary</button>
        <button class="btn btn-ghost">Ghost</button>
      </div>
    </div>

    <div class="code-block">
      <pre><code>&lt;button class="btn btn-primary"&gt;Primary&lt;/button&gt;
&lt;button class="btn btn-secondary"&gt;Secondary&lt;/button&gt;
&lt;button class="btn btn-ghost"&gt;Ghost&lt;/button&gt;</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>Sizes</h2>
    <p>Three sizes to fit different contexts and hierarchy.</p>

    <div class="demo-container">
      <div class="button-showcase">
        <button class="btn btn-primary btn-sm">Small</button>
        <button class="btn btn-primary btn-md">Medium</button>
        <button class="btn btn-primary btn-lg">Large</button>
      </div>
    </div>

    <div class="code-block">
      <pre><code>&lt;button class="btn btn-primary btn-sm"&gt;Small&lt;/button&gt;
&lt;button class="btn btn-primary btn-md"&gt;Medium&lt;/button&gt;  {# default #}
&lt;button class="btn btn-primary btn-lg"&gt;Large&lt;/button&gt;</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>With Icons</h2>
    <p>Buttons can include icons for additional visual context.</p>

    <div class="demo-container">
      <div class="button-showcase">
        <button class="btn btn-primary btn-icon-left">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14"/>
            <path d="M12 5l7 7-7 7"/>
          </svg>
          Continue
        </button>
        <button class="btn btn-secondary btn-icon-right">
          Download
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
            <polyline points="7 10 12 15 17 10"/>
            <line x1="12" y1="15" x2="12" y2="3"/>
          </svg>
        </button>
        <button class="btn btn-ghost btn-icon-only" aria-label="Settings">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="3"/>
            <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/>
          </svg>
        </button>
      </div>
    </div>

    <div class="code-block">
      <pre><code>&lt;button class="btn btn-primary btn-icon-left"&gt;
  &lt;svg&gt;...&lt;/svg&gt;
  Continue
&lt;/button&gt;

&lt;button class="btn btn-ghost btn-icon-only" aria-label="Settings"&gt;
  &lt;svg&gt;...&lt;/svg&gt;
&lt;/button&gt;</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>Persona Variants</h2>
    <p>Buttons can adopt persona colors for contextual actions.</p>

    <div class="demo-container">
      <div class="button-showcase">
        <button class="btn btn-persona btn-persona--ferni">Talk to Ferni</button>
        <button class="btn btn-persona btn-persona--maya">Talk to Maya</button>
        <button class="btn btn-persona btn-persona--peter">Talk to Peter</button>
      </div>
    </div>

    <div class="code-block">
      <pre><code>&lt;button class="btn btn-persona btn-persona--ferni"&gt;Talk to Ferni&lt;/button&gt;
&lt;button class="btn btn-persona btn-persona--maya"&gt;Talk to Maya&lt;/button&gt;
&lt;button class="btn btn-persona btn-persona--peter"&gt;Talk to Peter&lt;/button&gt;</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>States</h2>
    <p>Buttons provide visual feedback for different interaction states.</p>

    <div class="demo-container">
      <div class="button-showcase">
        <button class="btn btn-primary">Default</button>
        <button class="btn btn-primary" style="background: var(--accent-hover);">Hover</button>
        <button class="btn btn-primary" disabled>Disabled</button>
        <button class="btn btn-primary btn-loading">
          <span class="btn-spinner"></span>
          Loading
        </button>
      </div>
    </div>

    <div class="code-block">
      <pre><code>&lt;button class="btn btn-primary"&gt;Default&lt;/button&gt;
&lt;button class="btn btn-primary" disabled&gt;Disabled&lt;/button&gt;
&lt;button class="btn btn-primary btn-loading"&gt;
  &lt;span class="btn-spinner"&gt;&lt;/span&gt;
  Loading
&lt;/button&gt;</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>Full Width</h2>
    <p>Buttons can expand to fill their container.</p>

    <div class="demo-container" style="max-width: 400px; margin-left: auto; margin-right: auto;">
      <button class="btn btn-primary btn-block">Full Width Button</button>
      <div style="height: var(--space-3);"></div>
      <button class="btn btn-secondary btn-block">Secondary Full Width</button>
    </div>

    <div class="code-block">
      <pre><code>&lt;button class="btn btn-primary btn-block"&gt;Full Width Button&lt;/button&gt;</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>CSS Custom Properties</h2>

    <div class="tokens-table">
      <table>
        <thead>
          <tr>
            <th>Token</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>--accent-primary</code></td>
            <td>Primary button background</td>
          </tr>
          <tr>
            <td><code>--accent-hover</code></td>
            <td>Primary button hover state</td>
          </tr>
          <tr>
            <td><code>--border-medium</code></td>
            <td>Secondary button border</td>
          </tr>
          <tr>
            <td><code>--radius-md</code></td>
            <td>Button border radius</td>
          </tr>
          <tr>
            <td><code>--duration-fast</code></td>
            <td>Transition duration</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <section class="section">
    <h2>Full CSS</h2>

    <div class="code-block">
      <pre><code>.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  font-family: var(--font-body);
  font-weight: var(--font-weight-semibold);
  font-size: var(--text-sm);
  border-radius: var(--radius-md);
  padding: var(--space-3) var(--space-5);
  border: none;
  cursor: pointer;
  transition: all var(--duration-fast) var(--ease-ease-out);
  text-decoration: none;
}

/* Primary */
.btn-primary {
  background: var(--accent-primary);
  color: var(--text-inverse);
}

.btn-primary:hover {
  background: var(--accent-hover);
  transform: translateY(-1px);
}

/* Secondary */
.btn-secondary {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border-medium);
}

.btn-secondary:hover {
  border-color: var(--accent-primary);
  color: var(--accent-primary);
}

/* Ghost */
.btn-ghost {
  background: transparent;
  color: var(--text-secondary);
}

.btn-ghost:hover {
  background: var(--bg-secondary);
  color: var(--text-primary);
}

/* Sizes */
.btn-sm { padding: var(--space-2) var(--space-4); font-size: var(--text-xs); }
.btn-md { padding: var(--space-3) var(--space-5); font-size: var(--text-sm); }
.btn-lg { padding: var(--space-4) var(--space-6); font-size: var(--text-base); }

/* States */
.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

.btn-block {
  width: 100%;
}

/* Loading */
.btn-loading {
  pointer-events: none;
}

.btn-spinner {
  width: 16px;
  height: 16px;
  border: 2px solid transparent;
  border-top-color: currentColor;
  border-radius: 50%;
  animation: btn-spin 0.8s linear infinite;
}

@keyframes btn-spin {
  to { transform: rotate(360deg); }
}</code></pre>
    </div>
  </section>

  <section class="section">
    <h2>Accessibility</h2>
    <div class="guidelines-grid">
      <div class="guideline-card">
        <h4>Focus Indicators</h4>
        <p>All buttons have visible focus rings for keyboard navigation. Never remove <code>:focus-visible</code> styles.</p>
      </div>
      <div class="guideline-card">
        <h4>Icon-Only Buttons</h4>
        <p>Always include <code>aria-label</code> on icon-only buttons to describe the action.</p>
      </div>
      <div class="guideline-card">
        <h4>Loading States</h4>
        <p>Use <code>aria-busy="true"</code> during loading to announce state to screen readers.</p>
      </div>
    </div>
  </section>
</article>

<style>
.component-page {
  max-width: var(--container-max);
}

.breadcrumb {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-sm);
  color: var(--text-muted);
  margin-bottom: var(--space-4);
}

.breadcrumb a {
  color: var(--accent-primary);
  text-decoration: none;
}

.breadcrumb a:hover {
  text-decoration: underline;
}

.page-header h1 {
  font-family: var(--font-display);
  font-size: var(--text-4xl);
  font-weight: var(--font-weight-bold);
  letter-spacing: -0.02em;
  margin-bottom: var(--space-4);
}

.demo-container {
  background: var(--bg-secondary);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
  margin: var(--space-6) 0;
}

.button-showcase {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-4);
  flex-wrap: wrap;
}

/* Button Styles */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  font-family: var(--font-body);
  font-weight: var(--font-weight-semibold);
  font-size: var(--text-sm);
  border-radius: var(--radius-md);
  padding: var(--space-3) var(--space-5);
  border: none;
  cursor: pointer;
  transition: all var(--duration-fast) var(--ease-ease-out);
}

.btn-primary {
  background: var(--accent-primary);
  color: white;
}

.btn-primary:hover:not(:disabled) {
  background: var(--accent-hover);
  transform: translateY(-1px);
}

.btn-secondary {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border-medium);
}

.btn-secondary:hover:not(:disabled) {
  border-color: var(--accent-primary);
  color: var(--accent-primary);
}

.btn-ghost {
  background: transparent;
  color: var(--text-secondary);
  border: none;
}

.btn-ghost:hover:not(:disabled) {
  background: var(--bg-tertiary);
  color: var(--text-primary);
}

.btn-sm { padding: var(--space-2) var(--space-4); font-size: var(--text-xs); }
.btn-md { padding: var(--space-3) var(--space-5); font-size: var(--text-sm); }
.btn-lg { padding: var(--space-4) var(--space-6); font-size: var(--text-base); }

.btn-icon-only {
  padding: var(--space-3);
  width: 44px;
  height: 44px;
}

.btn-block {
  width: 100%;
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.btn-loading {
  pointer-events: none;
}

.btn-spinner {
  width: 16px;
  height: 16px;
  border: 2px solid transparent;
  border-top-color: currentColor;
  border-radius: 50%;
  animation: btn-spin 0.8s linear infinite;
}

@keyframes btn-spin {
  to { transform: rotate(360deg); }
}

/* Persona Variants */
.btn-persona {
  color: white;
}

.btn-persona--ferni { background: var(--persona-ferni); }
.btn-persona--maya { background: var(--persona-maya); }
.btn-persona--peter { background: var(--persona-peter); }
.btn-persona--alex { background: var(--persona-alex); }
.btn-persona--jordan { background: var(--persona-jordan); }
.btn-persona--nayan { background: var(--persona-nayan); }

.btn-persona:hover {
  filter: brightness(1.1);
  transform: translateY(-1px);
}

.code-block {
  background: var(--bg-tertiary);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  margin: var(--space-4) 0;
  overflow-x: auto;
}

.code-block pre {
  margin: 0;
}

.code-block code {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  line-height: var(--leading-relaxed);
  color: var(--text-primary);
}

.tokens-table {
  margin: var(--space-6) 0;
  overflow-x: auto;
}

.tokens-table table {
  width: 100%;
  border-collapse: collapse;
}

.tokens-table th,
.tokens-table td {
  text-align: left;
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--border-subtle);
}

.tokens-table th {
  font-weight: var(--font-weight-semibold);
  background: var(--bg-secondary);
}

.tokens-table code {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  background: var(--bg-tertiary);
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
}

.guidelines-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: var(--space-5);
  margin: var(--space-6) 0;
}

.guideline-card {
  background: var(--bg-secondary);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
}

.guideline-card h4 {
  font-family: var(--font-display);
  font-weight: var(--font-weight-semibold);
  margin-bottom: var(--space-2);
  color: var(--accent-text);
}

.guideline-card p {
  font-size: var(--text-sm);
  color: var(--text-muted);
  line-height: var(--leading-relaxed);
}

.guideline-card code {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  background: var(--bg-tertiary);
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
}

@media (max-width: 768px) {
  .page-header h1 {
    font-size: var(--text-3xl);
  }
}
</style>
