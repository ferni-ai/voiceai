{
  "name": "ferni-website",
  "version": "2.0.0",
  "description": "Ferni marketing website - design system integrated",
  "scripts": {
    "dev": "npm run sync:tokens && eleventy --serve --port=8080",
    "build": "npm run sync:tokens && eleventy",
    "build:prod": "npm run sync:tokens && eleventy && node scripts/minify.js",
    "minify": "node scripts/minify.js",
    "clean": "rm -rf _site",
    "preview": "npm run build && cd _site && python3 -m http.server 8080",
    
    "sync:tokens": "node ../../design-system/sync-promo-tokens.js && cp css/design-tokens.css src/css/_tokens.css",
    
    "deploy": "npm run build:prod && firebase deploy --only hosting:ferni-landing",
    "deploy:prod": "npm run build:prod && firebase deploy --only hosting:ferni-prod",
    "deploy:all": "npm run build:prod && firebase deploy --only hosting",
    
    "lint:design": "./scripts/lint-design.sh",
    "precommit": "npm run lint:design && npm run audit:content",
    
    "marketing:images": "node scripts/generate-assets.js",
    "marketing:optimize": "node scripts/optimize-assets.js",
    "marketing:optimize:images": "node scripts/optimize-assets.js --images",
    "marketing:optimize:videos": "node scripts/optimize-assets.js --videos",
    "marketing:audit": "./scripts/audit-content.sh",
    
    "audit:content": "./scripts/audit-content.sh",
    "audit:design": "./scripts/lint-design.sh",
    "audit:all": "npm run audit:design && npm run audit:content"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "clean-css-cli": "^5.6.3",
    "esbuild": "^0.27.1"
  }
}
